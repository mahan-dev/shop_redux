import { createContext } from "react";

export const ProductApiContextProvider = createContext();
const ProductApiContext = () => {

    return (
        <div>
            
        </div>
    );
};

export default ProductApiContext;